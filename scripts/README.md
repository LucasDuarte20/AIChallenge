# Scripts (Skeleton)

Ideas de scripts útiles:

- `scripts/generate_plan.sh`:
  - llama a `/clients/import-excel`
  - corre `/geocode/run` en loop (batches)
  - llama a `/plans/generate`
  - descarga `/plans/{code}/export.xlsx`

